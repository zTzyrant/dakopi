meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "login_id": "admin",
    "password": "rahasia"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
