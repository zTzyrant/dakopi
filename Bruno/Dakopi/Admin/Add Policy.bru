meta {
  name: Add Policy
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/admin/casbin/policy
  body: json
  auth: inherit
}

body:json {
  {
    "sub": "admin",
    "obj": "/api/v1/secret",
    "act": "GET"
  }
}
