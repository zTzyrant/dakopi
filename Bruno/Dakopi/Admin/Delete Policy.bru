meta {
  name: Delete Policy
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/api/admin/casbin/policy
  body: json
  auth: inherit
}

body:json {
  {
    "sub": "admin",
    "obj": "/api/v1/secret",
    "act": "GET"
  }
}
